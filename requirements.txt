# ── Red Sentinel — unified Python dependencies ──────────────
# All microservices share this single file.
# Pinned versions where the module authors specified them;
# range constraints kept where they were already loose.

# -- Web framework & server -----------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0

# -- HTTP / networking ----------------------------------------
httpx==0.27.0

# -- ML (context-module) -------------------------------------
torch==2.4.0
transformers==4.44.0
tokenizers==0.19.0

# -- ML (payload ranker) -------------------------------------
xgboost==2.0.3
scikit-learn==1.5.0
numpy==1.26.4

# -- Data -----------------------------------------------------
pandas==2.2.0

# -- HTML parsing ---------------------------------------------
beautifulsoup4==4.12.3
lxml==5.3.0

# -- Browser automation (fuzzer) ------------------------------
playwright>=1.48,<2.0

# -- Exploitable test site ------------------------------------
flask>=3.0,<4.0
